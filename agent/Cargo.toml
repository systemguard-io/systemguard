[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
notify = "6.1.1"
tokio = { version = "1", features = ["full"] }

reqwest = { version = "0.11", features = ["json", "blocking"] }
